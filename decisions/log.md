# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-04-18] DECISION: Build Gmail MCP inside a new `~/Projects/mcps/` monorepo rather than a standalone repo, and keep credentials + Claude registration at user level (`~/.mcps/<server>/` + `~/.claude.json`). | REASONING: Monorepo matches official Anthropic pattern (`modelcontextprotocol/servers`) and community best practice (Turborepo/npm workspaces). A standalone repo locks in a single-purpose layout; a monorepo lets every future personal MCP (Notion, Linear, etc.) share tsconfig, utilities, and one publishable GitHub project. Credentials stay out of any git repo so a committed secret is impossible. Registration at user level makes the MCP available in every Claude Code session, not just inside DionAi. | CONTEXT: First MCP server for Dionisis's setup. v1 scope locked to read-only + label management (5 tools) to avoid destructive operations on day 1; `send_email`, drafts, threading move to v2. Stack: Node 20+, TypeScript, @modelcontextprotocol/sdk v1.29, googleapis v171, zod, tsdown. GCP project `dion-mcp-tools` isolated from any production OAuth. Code at `~/Projects/mcps/servers/gmail`, DionAi tracker at `projects/gmail-mcp/`.

[2026-04-19] DECISION: Ship gmail-mcp Phase S + Phase I together, before Phase 2. | REASONING: Phase 2 introduces send + drafts, which is the first place AI can cause irreversible harm (wrong recipient, accidental send). Retrofitting secrets hardening + safety primitives after send/delete tools already exist is painful and risky. Bundling S and I means Phase 2 tools land with 10-layer defense already wired (sanitizer, Keychain, deny rules, block-env-bash hook, gmail-guard hook, ask permissions, elicitation helper, payload-hash two-phase commit, rate limiter, dry-run switch). Extracted into two shared packages (`@mcps/shared-secrets`, `@mcps/shared-safety`) so every future MCP in the monorepo inherits them for free. | CONTEXT: Monorepo `~/Projects/mcps`. Gmail v1 already shipped 2026-04-18 with on-disk JSONs; migration path is `bootstrap.sh gmail-mcp --delete-files`. Keychain chosen over `keytar` / 1Password to stay zero-deps and stronger (native `security` CLI + -T ACL grant means no confirmation dialogs, no extra Node module that could be tampered with). Plan doc: `projects/gmail-mcp/v2-plan.md`.

[2026-04-17] DECISION: Lock v3 as active Gemini prompt for M3 color-role analysis in video-templates. | REASONING: v3 produced 4/4 run consistency on template-126 (8/8/8/8 slot count, 12/12 text slots → on-primary, 0 orphan on-X violations, pink/purple/red all stable). v2's two regressions (text role copied text color, price labeled on-surface without a surface slot) are closed by v3's "text role = role of slot beneath" rule and "on-X requires X" self-check. | CONTEXT: Phase A of palette systematization goal. Artifacts in video-templates repo at reports/color-roles/template-analysis-prompt-v{1,2,3}.md and reports/2026-04-17-template-126-consistency.html. Next: scale to 5 templates and run the scorer.
